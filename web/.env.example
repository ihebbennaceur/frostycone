# Copy to .env and fill values
# Example connection string (uses integrated security on Windows). If this fails for you, use SQL authentication.
DB_CONN="Data Source=ZAK-PC;Initial Catalog=the frosty cone;Integrated Security=True;TrustServerCertificate=True"
# JWT secret for signing tokens
JWT_SECRET=change_this_to_a_strong_secret
# Server port
PORT=3000
